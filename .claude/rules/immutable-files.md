---
paths:
  - ".claude/commands/**/*.md"
  - ".claude/agents/**/*.md"
  - ".claude/skills/**/*.md"
---

# Immutable Framework Files

These files are part of the DCF (Design Cascading Framework) and MUST NOT be modified during normal project work.

## BLOCKED Actions
- DO NOT edit command files in `.claude/commands/`
- DO NOT edit agent files in `.claude/agents/`
- DO NOT edit skill files in `.claude/skills/`

## Why
These files define the DCF methodology itself. Changing them affects all future projects using this framework.

## If Changes Are Needed
Framework changes require explicit user request with clear justification. The user must specifically say they want to modify the framework, not just the project.
